import React from 'react'
import { View, Text } from 'react-native'

export default function NewWorkoutScreen() {
    return (
        <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}} >
            <Text>Screen with New Workout.</Text>
        </View>
    )
}
